# Artifact and schema tests

Owner: `EVIDENCE`

This area checks JSON schemas, manifest links, hashes, generator identity, and
deterministic artifact structure. Generated outputs are changed through their
generator, never by hand to make an audit pass.
