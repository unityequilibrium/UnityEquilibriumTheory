# UET Foundation Status Aggregate

This document summarizes the generated cross-family foundation boundary. It is a coordination artifact, not a physics proof.

Machine-readable source: [`uet_foundation_status_aggregate.json`](artifacts/uet_foundation_status_aggregate.json)

Regenerator: [`build_uet_foundation_status_aggregate.py`](../scripts/audit/build_uet_foundation_status_aggregate.py)

## Current result

- `audit_status`: `PASS`
- `foundation_status`: `BLOCKED`
- Gate counts: `7 BLOCKED`, `2 PASS_CONDITIONAL`
- Scoped legacy C/I operator and beta semantics: conditionally closed
- Global foundation: not closed

## Controlling blockers

1. Matter-space causal response fails the pre-arrival leakage gate.
2. The O(2)-to-legacy-double-well reduction fails its residual gate.
3. Topic and code equation inventories remain incomplete for correspondence purposes.
4. Code-only equation surfaces still require F1-F3 ontology, counterpart, and unit review.
5. Downstream observable and real-data gates remain blocked.

The legacy potential/derivative pair, information operator/source sign, and beta/Landauer separation are no longer listed as unresolved contradictions in the scoped canonical lane. The historical `legacy_local` path remains a quarantined comparator.

## Conditional lanes

- Einstein/GR: algebraic/local closed-limit evaluator only; not the Einstein field equations.
- Relativistic O(2): tree-level natural-unit EOS and ideal constitutive sector only.
- Matter-space: internal functional/ledger checks exist, but the full causal candidate remains blocked.
- Trace: derived history observable with no physical backreaction claim.

## Working rules

- A force is variational only when it is the derivative of the declared functional in the same boundary and unit lane.
- `C` has no universal physical identity; density, charge, and order-parameter mappings are lane-specific.
- Open-system language applies to an explicit effective subsystem, not automatically to the universe.
- A standard-theory special case requires a declared limit, compatible ontology/units, and residual or convergence evidence.
- No real-data claim is allowed before an observable operator, uncertainty, provenance, and holdout contract exists.

## Claim boundary

The aggregate supports only conditional, lane-specific internal statements. It does not establish a complete UET theory, global cosmic openness, an Einstein derivation, a dark-matter replacement, or particle/antimatter claims.