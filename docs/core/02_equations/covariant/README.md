# Covariant equation family

Canonical implementation area for the covariant response and transport
modules.  The scientific status remains controlled by the equation registry,
verifier artifacts, and foundation gates; moving a module here changes only
its source-of-truth path.

Legacy imports under `docs.core.uet_covariant_*` remain compatibility shims
until the family import and regression checkpoints are complete.
