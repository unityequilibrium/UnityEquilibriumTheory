# Equation families

This area contains canonical equation-family implementations. Numeric folder
names are organizational labels; callers should use the stable root facade or
the shared importlib compatibility loader.

Owner: EQUATION

Every family must link its formula contract, unit lane, verifier and generated
artifact before a physical claim can depend on it.
