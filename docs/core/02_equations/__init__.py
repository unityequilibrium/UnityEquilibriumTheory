"""Canonical equation families for the UET core.

The numbered directory is a documentation-facing namespace.  Canonical
modules are loaded through :mod:`importlib` so the public ``docs.core`` facade
and its legacy compatibility shims remain stable during migration.
"""
