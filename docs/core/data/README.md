# Legacy data compatibility boundary

Declared data is being migrated to docs/core/06_data and tooling is being
migrated to docs/scripts/core. Keep this directory only as a transition point
until all path consumers use the canonical resolver.
