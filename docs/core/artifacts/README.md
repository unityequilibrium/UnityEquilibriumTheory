# Legacy artifact compatibility boundary

Canonical generated artifacts belong in docs/core/07_artifacts. This directory
is retained temporarily for legacy paths and indexes only; new generators must
not write duplicate outputs here.
