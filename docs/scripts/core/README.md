# Core research tooling

Scripts supporting core research audits, data preparation, reporting, runners
and verification live here. They are tooling, not equation implementations or
declared source data.

The migration preserves relative subtrees and uses repository-root path
helpers so scripts do not depend on their caller's working directory.
