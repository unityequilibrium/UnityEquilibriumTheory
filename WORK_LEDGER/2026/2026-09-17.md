# Work Ledger — 2026-09-17

## Core physical migration closure

- Area: repo-ops (secondary: research-standards)
- Workspace: `docs/core` and `docs/scripts/audit`
- Scope: Consolidated the physical core layout into the numeric canonical tree, archived root/data/proof compatibility assets, refreshed the legacy module alias registry, repaired active canonical-path consumers, and regenerated the migration/organization control artifacts.
- Files/artifact groups: canonical core paths, compatibility alias registry, migration manifest/report, organization registry and dependency graph, closure packet, path/import/link/data-tooling audits, and the all-waves completion audit.
- Verification: physical migration audit PASS (`duplicate_targets=0`, `missing_current_paths=0`, `redirect_errors=0`); path audit PASS; import audit PASS (`181/181`); active-link audit PASS (`245/245`); data/tooling audit PASS; combined migration audit PASS; closure completion audit PASS_WITH_FOUNDATION_PHYSICS_BLOCKED; closure regression tests `2 passed`.
- Result: `docs/core` root is reduced to six protected entrypoints; `426` legacy root/data/proof assets are archived behind machine-readable compatibility records; old `data`, `test`, `02_Proof`, and `artifacts` boundaries no longer contain active implementations; physical migration is complete and physics status is unchanged.
- Public safety: partial
- Remaining: three registry review/quarantine records remain explicit; foundation and scientific-link gates remain BLOCKED; compatibility/history records intentionally retain legacy provenance strings; unrelated dirty worktree changes remain outside this scope.
- Next action: stage and commit only the migration files, then keep the branch local until the user gives separate push/PR instruction.

## Explicit migration-completion state

- Area: repo-ops (secondary: research-standards)
- Workspace: `docs/core` and `docs/scripts/audit`
- Scope: Updated the physical migration planner and registry reconciler so completion, pending move targets, current-run movement, and last successful consolidation are separate machine-readable fields.
- Files/artifact groups: planner/reconciler scripts, physical migration manifest/report, organization index, registry, migration map, dependency graph, and reconciliation artifacts.
- Verification: planner reports `physical_migration_complete=true`, `files_to_move=0`, and last consolidation `PASS` with 426 archived compatibility assets; core physical/path/import/link/data-tooling audits remain PASS.
- Result: The canonical index now states why no move occurs in a rerun while proving that the prior physical consolidation completed; physics status remains unchanged.
- Public safety: partial
- Remaining: three explicit quarantine records and scientific/foundation blockers remain; unrelated dirty worktree changes and five pre-existing untracked physics artifacts remain outside this commit.
- Next action: commit this scoped control-plane clarification only; keep branch local and do not push/PR without explicit instruction.

## Publish reconciliation — main-based migration branch

- Area: repo-ops (secondary: research-standards)
- Workspace: isolated `codex/research/core-physical-migration-main` worktree
- Scope: reconciled the publish tree against the tracked migration scope, removed six untracked artifact records from the publish inventory, synchronized canonical tooling hashes, restored two tracked Topic 13 compatibility helpers required for migrated regression collection, and refreshed canonical organization outputs.
- Verification: physical/path/import/link/data-tooling migration audits PASS; deterministic registry reconciliation check PASS; targeted core migration tests `67 passed`.
- Result: publish branch contains migration commits based on remote `main`; unrelated dirty research and book changes remain excluded from the branch.
- Public safety: partial; foundation gate remains `BLOCKED`, registry review/quarantine remains explicit, and no physics claim was promoted.
- Remaining: required GitHub PR checks must run before any merge to protected `main`.
- Next action: push the isolated branch and open the scoped migration PR requested by the user.

## Canonical-path CI repair

- Area: repo-ops (secondary: research-standards)
- Workspace: isolated `codex/research/core-physical-migration-main` worktree
- Scope: repaired cross-platform canonical path resolution and the Topic 0.11 Noether generator's dependency paths.
- Files/artifact groups: `docs/core/core_paths.py` and the Topic 0.11 dependency-gate generator; no generated artifact was hand-edited.
- Verification: diff review and path-contract inspection completed; PR checks must rerun on the pushed commit.
- Public safety: partial; no raw/private data or physics claim was changed.
- Remaining: canonical artifact consumer compatibility and external-source availability remain open blockers.

## 2026-09-17 — Core migration CI repair: canonical artifact regeneration

- Area: repo-ops (secondary: research-standards)
- Workspace: isolated `codex/research/core-physical-migration-main` worktree
- Files/artifact groups: canonical core verification/correspondence/gate artifacts, Topic 0.11 matter-space/Noether generators and regression tests, migration update log.
- Verification: targeted core equation/regression suite `59 passed`; `git diff --check` clean.
- Public safety: partial; only path/hash reproducibility and generated outputs changed; no raw/private data or physics claim was promoted.
- Remaining: PR-wide Python validation and Topic 13/public-source boundary checks remain open.
- Next action: commit and push this scoped packet, then inspect the next GitHub check run.

## 2026-09-17 — Core migration CI repair: public boundaries and curved artifacts

- Area: repo-ops (secondary: research-standards)
- Workspace: isolated `codex/research/core-physical-migration-main` worktree
- Scope: repaired metadata-only public-source boundaries for unavailable Topic 13 raw inputs, canonical artifact consumers, and stale curved 3+1 source hashes; regenerated dependent gates through the official generators.
- Verification: Topic 13/dependency generators PASS; curved 3+1 regression `27 passed`; full core suite `2170 passed, 6 warnings`; `git diff --check` clean.
- Public safety: partial; raw/private source payloads were not published and no physics claim was promoted.
- Remaining: commit/push and required GitHub PR checks; foundation and external-data blockers remain explicit.
- Next action: stage only the publish repair scope, commit, push the existing PR branch, and inspect CI before merge.
