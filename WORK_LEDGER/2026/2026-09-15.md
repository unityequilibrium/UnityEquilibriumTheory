# 2026-09-15

### Core physical migration v3 — Wave 5g resource-selection contract
- area: repo-ops / research-standards
- workspace: docs/core/artifacts → docs/core/07_artifacts/provenance
- changed: switched the dynamic-game contract generator to canonical path authority; added planner multiline-assignment regression coverage and exact-move migration coverage; refreshed physical and artifact control-plane metadata
- verification: focused migration/history/planner tests PASS (5); artifact planner/audit PASS; physical/path audits PASS after metadata regeneration; scientific-link check PASS with existing blocked scientific boundary; physics status changes 0
- public-safety: partial; one additional organization artifact moved with unchanged SHA-256; foundation remains BLOCKED; no physics claim changed
- uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; the separate Topic 0.13 preflight candidates were restored and not included
- next: create the scoped Wave 5g commit, then select the next generator-confirmed artifact family; do not overwrite legacy outputs on provenance drift
### Wave 5g post-commit metadata consistency correction
- area: repo-ops / research-standards
- workspace: core artifact migration control plane
- changed: refreshed artifact manifest, review queue, and audit after the scientific-link input hash update; removed immediate post-commit planner drift
- verification: planner --check PASS; queue --check PASS; artifact audit --check PASS; migrated count remains 5; physics status changes 0
- public-safety: partial; metadata-only correction; foundation remains BLOCKED and unrelated dirty files remain untouched
- next: no new migration is staged; choose the next generator-confirmed family only after its exact-payload preflight