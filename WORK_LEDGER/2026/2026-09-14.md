# Work Ledger — 2026-09-14

## Core organization Wave 1 disposition

- Area: `research-core`; core file organization and research routing.
- Workspace: `docs/core/`; owner/family assignment and quarantine control plane.
- Changed: extended `uet_research_organization_policy.json` with ten deterministic disposition rules; updated the v2 registry generator; added Wave 1 regression tests; regenerated the core manifest, organization registry, migration map, audit, and governance index; updated the core organization log.
- Verification: core manifest refreshed to 1,545 files; v2 generator `--check` PASS; six targeted organization tests PASS; policy and JSON artifacts parse successfully.
- Result: 133 pre-wave review records classified, 133 assigned, 0 unassigned, 0 quarantined; foundation status remains `BLOCKED`; physical migration remains false.
- Public-safety status: `safe` organization metadata only; no equation or physics claim promoted and no unrelated dirty files staged.
- Uncommitted/private remainder: unrelated changes in root `AGENTS.md`, foundation artifacts, and book workflow files remain outside this wave; no raw/private data was added.
- Next action: scientific-link review by family, followed by import/link/compatibility scans before any physical migration.

## Core scientific-link Wave 2 audit

- Area: `research-core`; core equation/family linkage and organization routing.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: added `audit_uet_core_scientific_links.py`, generated `uet_core_scientific_link_audit.json`, added three regression checks, and synchronized core workflow/update-log references.
- Verification: 133 assigned records audited across 9 pending families; 133 source paths exist; 0 canonical family-contract matches; all five required link fields are absent from the assigned records; correspondence JSON is exact-parseable with one case-insensitive key lint; foundation remains `BLOCKED`; physical move remains false.
- Public-safety status: `blocked` scientific-link state; no equation, unit, observable, or empirical claim promoted.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; no raw/private data added.
- Next action: build canonical family-link records in bounded foundation batches, beginning with matter-space/covariant contracts, then rerun this audit before any downstream promotion or physical move.

## Core organization Wave 3 bounded matter-space flux family link

- Area: `research-core`; bounded equation-family correspondence and organization linkage.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: added the matter-space flux correspondence addendum with four formula records; merged it into the central registry; declared `core.matter_space_flux`; added explicit formula, unit, verifier, artifact, and claim links; added the bounded-family regression test; synchronized core standard and AGENTS.
- Verification: flux telegraph verifier `PASS`; coupled C/Phi verifier `PASS`; 19 targeted tests `PASS`; organization registry `--check` `PASS`; scientific-link audit `--check` `PASS`; foundation audit `PASS` with foundation gate still `BLOCKED`.
- Result: 2 of 133 assigned records now have a complete scientific-link chain; 131 records across 8 pending families remain open; no physical move or claim promotion occurred.
- Public-safety status: `blocked`; normalized internal branch only, with no SI or external-data claim.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; the earlier large code-surface refresh was separated and restored to its prior checkpoint; no raw/private data added.
- Next action: create the next bounded `core.covariant_pending` link package only after this family-link pattern is reviewed; keep physical migration deferred.

## Core organization Wave 4 bounded covariant-parent family link

- Area: `research-core`; bounded covariant equation-family correspondence and organization linkage.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: declared `core.covariant_parent` for `uet_covariant_parent.py`; added the dedicated `W2-EQUATION-COVARIANT-PARENT` disposition; regenerated the family contract, core manifest/index, organization registry/migration map/audit, and scientific-link audit; added the parent-link regression test.
- Verification: existing parent artifact `covariant_parent_verification.json` is `PASS`; 17 targeted tests PASS; manifest `--check`, organization registry `--check`, and scientific-link audit `--check` PASS; foundation audit remains `BLOCKED`.
- Result: 3 of 133 assigned review records now have the full formula/unit/verifier/artifact/claim link set; 130 records across 8 family groups remain unlinked; one case-insensitive metadata collision remains visible.
- Public-safety status: `blocked`; natural-unit conservative parent traceability only, with no GR, SI, open-system, or empirical promotion.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; no raw/private data added and no physical file move performed.
- Next action: select the next covariant sub-family only when its existing formula, verifier, artifact, and claim boundary can be linked without inventing evidence; keep the remaining pending modules blocked.

## Core organization Wave 5 bounded covariant-diffusion family link

- Area: `research-core`; bounded covariant-current correspondence and organization linkage.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: added the generated covariant-diffusion correspondence addendum with four formula records; merged those records into the central correspondence registry; declared `core.covariant_diffusion`; added the dedicated organization disposition, family-link generator, and regression tests; refreshed organization and scientific-link artifacts.
- Verification: existing diffusion verification remains `PASS` with `PARTIAL` evidence; 51 focused diffusion/parent/flux/organization/link tests PASS; central JSON and source hashes are checked by the generated addendum; physics foundation remains `BLOCKED`.
- Result: 4 of 134 assigned review records now have the full formula/unit/verifier/artifact/claim link set; 130 records across 8 family groups remain open; the case-insensitive `g_munu`/`G_munu` metadata lint remains review-required; no physical move occurred.
- Public-safety status: `blocked`; normalized constitutive current bridge traceability only, with microscopic transport, UV causality, KMS, SI, and physical-validation claims explicitly withheld.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; no raw/private data added and no physical file move performed.
- Next action: choose one more bounded family with an existing complete evidence chain, likely `core.noether_mapping`; do not promote the diffusion branch or widen into all covariant pending modules.

## Core organization Wave 6 bounded Noether-mapping family link

- Area: `research-core`; bounded O(2) Noether-to-hydrodynamic correspondence and organization linkage.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: added the generated Noether correspondence addendum with seven formula records; merged it into the central correspondence registry; declared `core.noether_mapping` formula/unit/verifier/artifact/claim links; added the dedicated organization disposition, link generator, and regression tests; refreshed the family contract, core manifest/index, organization registry/migration map/audit, and scientific-link audit.
- Verification: existing Noether state-map artifact remains `PASS` at the fixed-scale coordinate layer, formula audit remains `WARN`, dependency gate remains `BLOCKED`; 50 focused tests PASS; organization registry `--check` PASS; scientific-link audit `--check` PASS; foundation audit `PASS` with foundation gate still `BLOCKED`; compatibility audit `PASS` with compatibility status still `BLOCKED`.
- Result: 6 of 136 assigned review records now have the full formula/unit/verifier/artifact/claim link set; 130 records across 8 pending family groups remain open; the `g_munu`/`G_munu` metadata lint remains review-required; no physical file move or claim promotion occurred.
- Public-safety status: `blocked`; lane-specific O(2) charge-coordinate traceability only, with microscopic inversion, O(2) EOS/transport, SI, and external-validation claims explicitly withheld.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; no raw/private data added and no physical file move performed.
- Next action: select one further bounded family only from an existing complete evidence chain; keep Noether EOS, covariant transport, coarse-graining, and observable gates as the family controller.

## Core organization Wave 7 bounded derived-trace family link

- Area: `research-core`; bounded derived history-trace correspondence and organization linkage.
- Workspace: `docs/core/` and `docs/scripts/audit/`.
- Changed: linked the existing `uet.trace.derived_observable` central record to `core.trace`; added `W2-EQUATION-TRACE`; declared the existing trace test/artifacts in the family contract; added trace-link regression tests; refreshed the family contract, manifest/index, organization registry/migration map/audit, and scientific-link audit.
- Verification: existing trace artifact remains `internal_gate_status=PASS` with overall `WARN`, normalized units checked and SI open; formula audit remains `WARN`; bounded trace/link tests PASS; organization registry `--check` PASS; scientific-link audit `--check` PASS; foundation gate remains `BLOCKED`.
- Result: 7 of 137 assigned review records now have the full formula/unit/verifier/artifact/claim link set; 130 records across 8 pending family groups remain open; the `g_munu`/`G_munu` metadata lint remains review-required; no physical file move or claim promotion occurred.
- Public-safety status: `blocked`; derived normalized trace traceability only, with continuum causal-support, dimensional measurement, energy-reservoir, and external-validation claims explicitly withheld.
- Uncommitted/private remainder: unrelated root/foundation/book changes remain unstaged; no raw/private data added and no physical file move performed.
- Next action: select one further bounded family only from an existing complete evidence chain; keep trace compact-support and dimensional measurement as the family controller.

## Core physical migration v3 — Wave 0–2

- Area: esearch-core / repository organization.
- Workspace: docs/core/, docs/scripts/audit/.
- Changed: created canonical path/import helpers, physical migration manifest/report/audit, root navigation sync, canonical folder skeleton, and compatibility policy; physically migrated contracts, governance-support, Topic 13 support and history documents; retained old Markdown paths as redirects.
- Verification: migration planner target check PASS; physical migration audit PASS; docs.core import PASS; organization regression 3 passed; no physics status changes.
- Public-safety status: partial; physical organization and traceability improved, but foundation gate remains BLOCKED.
- Uncommitted/private remainder: existing user changes remain unstaged; Python, tests, data/tooling and generated artifacts are intentionally held for later consumer checkpoints.
- Next action: update canonical manifest/generator consumers, then migrate data/tooling and tests in separate scoped waves.

### Core physical migration v3 — control-plane hardening
- area: repo-ops / research-standards
- workspace: docs/core physical migration
- changed: canonical resolver collision rules, core AGENTS physical-tree rules, migration manifest/report/audit refresh
- verification: planner --check PASS; physical migration audit PASS; helper compile PASS
- public-safety: safe; no physics status changes; user-dirty files preserved
- next: data/tooling consumer path audit, then staged tooling migration

### Core physical migration v3 — Wave 3a tooling safe subset
- area: repo-ops / research-standards
- workspace: docs/core/data/scripts → docs/scripts/core
- changed: 25 canonical tooling moves, old-path shims/redirects, tooling manifest/audit, navigation controller
- verification: tooling migration audit PASS; compile and shim-target checks PASS; physical migration audit PASS
- public-safety: safe; 86 path-sensitive/output assets quarantined; no source data or physics status changed
- next: repair path bootstrap and provenance-map the remaining tooling/data before the next physical move

### Core physical migration v3 — Wave 3b source packages
- area: repo-ops / research-standards
- workspace: docs/core/data/external → docs/core/06_data/source_packages
- changed: 20 source-package files moved; governance redirect index and legacy README boundary added
- verification: source-package hash/redirect audit PASS; no raw legacy duplicate; global migration audit PASS
- public-safety: safe; source bytes unchanged; physics status unchanged; consumer paths remain a tracked follow-up
- next: consumer/path reference audit and quarantined tooling bootstrap repair
