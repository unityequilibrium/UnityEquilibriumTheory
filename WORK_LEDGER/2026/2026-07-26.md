# Work Ledger - 2026-07-26

## Foundation-first equation research workflow repair

- Area: `research-standards` (secondary: `research-core`)
- Scope: repository standards, `docs/core/` equation workflow, registry, dependency gate, and audit tooling
- Work: Added the canonical foundation-first F0-F8 equation research standard, folder-scoped core instructions, an initial core equation correspondence registry, a machine-readable protocol, and a blocked foundation dependency gate. Narrowed the legacy core README boundary and linked the new protocol from the existing standards workflow.
- Verification: `.venv\Scripts\python.exe docs/scripts/audit/audit_uet_equation_foundation.py --json`; audit schema/path result `PASS`, foundation gate result `BLOCKED` with controller `foundation_inventory_and_correspondence_registry_incomplete`; `git diff --check` returned no whitespace errors.
- Public-safety: `blocked` for physical promotion; documentation and workflow repair are safe, while the registry is explicitly marked `INITIAL_SEED_NOT_EXHAUSTIVE`.
- Files/artifacts: `AGENTS.md`, `docs/core/AGENTS.md`, `docs/core/README.md`, `docs/core/07_artifacts/correspondence/uet_equation_correspondence_registry.json`, `docs/core/07_artifacts/archive/uet_equation_research_protocol.json`, `docs/core/07_artifacts/gates/uet_foundation_dependency_gate.json`, `docs/scripts/audit/audit_uet_equation_foundation.py`, and linked `For Work` standards.
- Remaining: complete the F0 inventory across numbered topic equations, build the full UET-to-standard-physics correspondence matrix, and keep all downstream claims at the current blocked/diagnostic ceiling.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.

## Foundation compatibility and limiting-case audit

- Area: `research-core` (secondary: `research-standards`)
- Scope: declared legacy equations, implementation correspondence, units, standard-theory limits, and foundation principles
- Work: Added a generated compatibility gate, an audit regression test, and `UET_FOUNDATION_COMPATIBILITY_AUDIT.md`. The audit separates implementation contradictions from unresolved physical correspondence and conditional limiting cases.
- Verification: `.venv\Scripts\python.exe docs/scripts/audit/audit_uet_foundation_compatibility.py`; audit execution `PASS`, compatibility gate `BLOCKED`; 15 findings include the legacy potential/derivative contradiction, legacy information-operator conflict, beta/Landauer unit conflict, matter-space causal leakage blocker, and rejected O(2)-to-double-well reduction.
- Public-safety: `blocked` for physical promotion; GR closed limit and O(2) EOS remain `COMPATIBLE_CONDITIONAL` only.
- Files/artifacts: `docs/scripts/audit/audit_uet_foundation_compatibility.py`, `docs/core/07_artifacts/gates/uet_foundation_compatibility_gate.json`, `docs/core/UET_FOUNDATION_COMPATIBILITY_AUDIT.md`, and `docs/core/test/test_foundation_compatibility.py`.
- Remaining: repair the legacy conflicts, complete equation inventory/correspondence, and repair matter-space pre-arrival leakage before any downstream real-data claim.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## F0 equation inventory and cross-topic correspondence matrix

- Area: `research-standards` (secondary: `research-core`)
- Scope: all numbered-topic `FORMULA_AUDIT.md` registries and selected foundation/dependency rows
- Work: Added generated F0 inventory for 27 formula-audit files and 260 rows, plus a 19-row critical correspondence matrix separating standard counterpart, UET derivation status, and special-case status.
- Verification: `build_uet_equation_inventory.py` returned `PASS_WITH_DISCLOSED_GAPS` with inventory gate `BLOCKED`; no duplicate formula IDs; inventory and matrix regression suite passed 5 tests.
- Public-safety: `blocked` for foundation promotion; topic 0.1 remains scaffold-blocked, standard formulas remain baselines, and UET bridges remain open unless separately derived.
- Files/artifacts: `docs/scripts/audit/build_uet_equation_inventory.py`, `docs/core/07_artifacts/gates/uet_foundation_equation_inventory.json`, `docs/scripts/audit/build_uet_foundation_correspondence_matrix.py`, `docs/core/07_artifacts/gates/uet_foundation_correspondence_matrix.json`, `docs/core/UET_FOUNDATION_EQUATION_INVENTORY.md`, `docs/core/UET_FOUNDATION_CORRESPONDENCE_MATRIX.md`, and related tests.
- Remaining: complete code-only formula inventory, close correspondence/observable maps, and audit selected open rows with equation residuals and unit contracts.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Special-case reproducibility repair

- Area: `research-core`
- Scope: O2 finite-density EOS, covariant superfluid transport, GR program gate, and limiting-case regression suite
- Work: Regenerated the five Wave-10 artifacts through `audit_uet_o2_superfluid_transport.py` after detecting inconsistent `generated_at` provenance in the stored program gate. No physics status was upgraded.
- Verification: O2/GR/matter-space special-case suite now passes `36 tests`; GR closed-limit verifier remains `PASS` algebraic/local, O2 program remains `BLOCKED`, and matter-space verifier remains `FAIL` at `prearrival_leakage`.
- Public-safety: `blocked` for physical promotion; this wave repairs reproducibility metadata only.
- Files/artifacts: generated O2/transport/program artifacts and refreshed compatibility/matrix inputs.
- Remaining: preserve generated-at/source snapshot consistency in future artifact waves and continue unresolved correspondence/causal gates.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Code-only equation surface inventory

- Area: `research-core` (secondary: `research-standards`)
- Scope: 32 core implementation/proof Python modules, excluding data/test/scripts support trees
- Work: Added a conservative static inventory of 1,450 equation-like code surfaces and 25 core files without registry path links. Every candidate is explicitly `REVIEW_REQUIRED`, ontology/unit/derivation `OPEN`, and `NOT_EVIDENCE`.
- Verification: `build_uet_code_surface_inventory.py` returned `PASS_WITH_DISCLOSED_GAPS` with inventory gate `BLOCKED`; static scan is not treated as equation proof.
- Public-safety: `blocked` for foundation promotion; the scan prevents omission but does not classify physical meaning automatically.
- Files/artifacts: `docs/scripts/audit/build_uet_code_surface_inventory.py`, `docs/core/07_artifacts/archive/uet_code_surface_inventory.json`, `docs/core/UET_CODE_SURFACE_INVENTORY.md`, `docs/core/test/test_uet_code_surface_inventory.py`, and core reading rules.
- Remaining: classify candidate surfaces into core equation, constitutive/application, standard baseline, numerical detail, or legacy/quarantine; then close F1-F3 for the true core subset.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Core equation-family ownership contract

- Area: `research-core` (secondary: `research-standards`)
- Scope: 32 core implementation modules and their equation-family/support ownership
- Work: Added a generated F1-F3 family contract assigning all 32 modules to 9 equation families plus 3 support/contract groups. Each family now declares ontology variables, standard counterpart, unit lane, derivation class, mathematical status, special-case status, and claim ceiling.
- Verification: `build_uet_core_equation_family_contract.py` returned `PASS` structurally with `32/32` module assignment and contract status `BLOCKED` from upstream gates; family-contract regression tests added.
- Public-safety: `blocked` for theory promotion; assignment closes ownership ambiguity but does not prove any family physically.
- Files/artifacts: `docs/scripts/audit/build_uet_core_equation_family_contract.py`, `docs/core/07_artifacts/archive/uet_core_equation_family_contract.json`, `docs/core/UET_CORE_EQUATION_FAMILY_CONTRACT.md`, and related tests/core instructions.
- Remaining: close F1-F3 for the equation families with open lanes, then perform family-specific formal/numerical residual checks and observable mapping.
- Commit status: local changes not yet staged; preserve unrelated worktree changes.
## Foundation status aggregate and stopping boundary

- Area: `research-standards` (secondary: `research-core`)
- Scope: cross-family F0-F8 status, conditional special cases, and foundation stopping criteria
- Work: Added a generated aggregate artifact and report that combine inventory, correspondence, compatibility, family ownership, matter-space, GR, O2, and trace evidence. Linked the aggregate into the core instructions and README.
- Verification: aggregate `audit_status=PASS`, `foundation_status=BLOCKED`, with `8 BLOCKED` and `1 PASS_CONDITIONAL` gates; targeted foundation/special-case suite passed `48 tests`; all aggregate input audits regenerated successfully.
- Public-safety: `blocked` for physical promotion; conditional GR/O2/comparator limits remain narrow and do not establish a universal UET theory.
- Files/artifacts: `docs/scripts/audit/build_uet_foundation_status_aggregate.py`, `docs/core/07_artifacts/gates/uet_foundation_status_aggregate.json`, `docs/core/UET_FOUNDATION_STATUS_AGGREGATE.md`, `docs/core/test/test_foundation_status_aggregate.py`, plus core workflow links.
- Remaining: close family-specific F1-F7 blockers in dependency order; do not start downstream physical claims while contradictions, unit/correspondence gaps, or causal leakage remain.
- Commit status: this aggregate wave is ready for scoped local commit; unrelated worktree changes remain unstaged.

## Legacy variational closure audit

- Area: `research-core` (secondary: `research-standards`)
- Scope: exact functional/gradient consistency of the legacy `C` potential and `I` coupling
- Work: Added a generated audit that evaluates the coded potential derivative by finite difference and checks the information-source sign implied by the declared `+beta*C*I` coupling.
- Verification: audit `audit_status=PASS`, `closure_status=BLOCKED`; potential derivative residual `13.675000000367197` against `1e-8`; information source expected sign `-1` but coded sign `+1`; aggregate now reports 9 controlling blockers; legacy/core regression suite remains the controlling evidence.
- Public-safety: `blocked`; legacy engine remains unchanged and quarantined as comparator.
- Files/artifacts: `docs/scripts/audit/audit_uet_legacy_variational_closure.py`, `docs/core/07_artifacts/gates/uet_legacy_variational_closure.json`, `docs/core/test/test_legacy_variational_closure.py`, and aggregate/core links.
- Remaining: repair or explicitly relabel the legacy functional/gradient pair before using it as the foundation for a standard-theory limit.
- Commit status: ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Special-case limit and provenance revalidation

- Area: `research-core` (secondary: `research-standards`)
- Scope: matter-space, thermal/phase pilots, covariant GR/O2 limits, and dependency hash chains
- Work: Regenerated core and pilot artifacts from their source verifiers after the legacy closure audit; refreshed thermal/phase pilot metadata and report-alignment hashes so the dependency chain is reproducible.
- Verification: special-case/core regression suite passed `111 tests`; program gate remains `BLOCKED` by `core_prearrival_leakage`, with `16/17` core gates passing, pilot artifact integrity `PASS`, and layout `WARN`.
- Public-safety: `blocked` for physical promotion; GR/O2 remain conditional lanes, thermal remains `SIMULATION_ONLY`, phase remains `INTERNAL_DIAGNOSTIC`.
- Files/artifacts: regenerated core/special-case/pilot artifacts and `docs/core/07_artifacts/gates/matter_space_report_alignment_gate.json`.
- Remaining: causal response repair and physical observable/unit maps; no standard-theory or real-data claim is promoted by this revalidation.
- Commit status: ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Correspondence matrix provenance refresh

- Area: `research-standards` (secondary: `research-core`)
- Scope: matrix hash linkage after core verifier regeneration
- Work: Regenerated the foundation correspondence matrix so its matter-space evidence snapshot matches the current verifier artifact.
- Verification: matrix audit `audit_status=PASS`, `matrix_status=BLOCKED`; no compatibility classification changed.
- Public-safety: `blocked`; this was provenance synchronization only.
- Files/artifacts: `docs/core/07_artifacts/gates/uet_foundation_correspondence_matrix.json`.
- Remaining: foundation inventory/correspondence gaps and family-specific F1-F7 blockers remain controlling.
- Commit status: local commit `4bc9762f0` created; unrelated worktree changes remain unstaged.
## Causal discretization classification

- Area: `research-core` (secondary: `research-standards`)
- Scope: causal-cone failure classification for `matter_space_coupled_v1`
- Work: Added a generated diagnostic showing that the current Heun/RK2 two-stage nearest-neighbor update has an implementation-level support-speed upper bound of `25` versus declared response speed `1` in the locked pulse run.
- Verification: diagnostic `audit_status=PASS`, original causal gate remains `FAIL` with leakage `0.017639381271029236` against `1e-6`; diagnostic test passed `3 tests`.
- Public-safety: `blocked`; this classifies a numerical support failure and does not validate or reject the continuum equation.
- Files/artifacts: `docs/scripts/audit/audit_matter_space_causal_discretization.py`, `docs/core/07_artifacts/archive/matter_space_causal_discretization_diagnostic.json`, test, aggregate links.
- Remaining: implement a characteristic/staggered or separately proven discrete-cone scheme and rerun the original gate without padding/clipping.
- Commit status: ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Strict-CFL causal reference lane

- Area: `research-core` (secondary: `research-standards`)
- Scope: narrow numerical reference for the matter-space Phi response, with frozen `C`
- Work: Added `causal_linear_space_step`, a strict-CFL centered recurrence, and a generated reference artifact. The nearest-neighbor recurrence has compact discrete support in the tested linearized frozen-C lane; the default full coupled Heun/RK2 candidate remains separately blocked.
- Verification: causal reference audit `PASS`; pre-arrival leakage fraction `0.0` in the reference lane and arrival response `0.9488839472937474`; targeted reference/discretization/core/legacy regression suite passed `34 tests`; aggregate remains `BLOCKED` with `F6` explicitly recording the reference as narrow evidence.
- Public-safety: `blocked` for physical promotion; this is an internally verified numerical control lane, not a proof of the continuum equation or full coupled dynamics.
- Files/artifacts: `docs/core/uet_matter_space.py`, `docs/scripts/audit/audit_matter_space_causal_reference.py`, `docs/core/07_artifacts/verification/matter_space_causal_reference_verification.json`, `docs/core/test/test_matter_space_causal_reference.py`, aggregate builder/artifact/report, and core instructions.
- Remaining: integrate a causality-preserving scheme into a fully coupled operator only after energy, conservation, convergence, and observable gates are re-established; do not pad the original cone or promote the default candidate.
- Commit status: this reference wave is ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Foundation compatibility decision matrix

- Area: `research-standards` (secondary: `research-core`)
- Scope: final separation of mathematical consistency, standard-physics correspondence, and old-theory special-case status
- Work: Added a generated decision artifact/report covering 12 core families, 260 inventoried topic formula rows, 8 locked principles, 12 core-family rows plus cross-cutting rows, and explicit hard contradictions, conditional limits, and coverage boundaries.
- Verification: decision builder `audit_status=PASS`; compatibility decision remains `BLOCKED_BY_LEGACY_CONTRADICTION`, standard correspondence `PARTIAL_CONDITIONAL_NOT_GLOBAL`, old-theory nesting `CONDITIONAL_ONLY`, and overall foundation `FOUNDATION_NOT_CLOSED`; the decision regression suite passed `17 tests`.
- Public-safety: `blocked` for universal physical promotion; the report distinguishes actual contradiction from missing evidence and does not call unresolved physics false.
- Files/artifacts: `docs/scripts/audit/build_uet_foundation_compatibility_decision.py`, `docs/core/07_artifacts/gates/uet_foundation_compatibility_decision.json`, `docs/core/UET_FOUNDATION_COMPATIBILITY_DECISION.md`, core README/instructions, and decision tests.
- Remaining: repair/quarantine legacy contradictions, close code-level correspondence and units, verify full coupled dynamics, then build observable/holdout evidence.
- Commit status: this decision wave is ready for a scoped local commit; unrelated worktree changes remain unstaged.
## Canonical legacy variational lane closure

- Area: `research-core` (secondary: `research-standards`)
- Scope: opt-in `legacy_variational_v1` functional/derivative and information-source contract
- Work: Made `potential_derivative` the exact radial derivative of the declared `potential_V`; preserved the historical shifted relation as named `legacy_reaction_derivative`; added the canonical negative information-source sign and strict guard against unsupported exchange/inertial terms.
- Verification: canonical closure audit `PASS_CONDITIONAL`; canonical finite-difference residual `3.671978276997834e-10` against `1e-8`; legacy comparator residual `13.675000000367197`; compatibility/aggregate/decision artifacts regenerated; scoped regression suite passed `36 tests`.
- Public-safety: `blocked` for global foundation/physical promotion; only the normalized opt-in lane is conditionally closed, while `legacy_local` remains a quarantined comparator.
- Files/artifacts: `docs/core/uet_master_equation.py`, variational/compatibility audit scripts, generated closure/compatibility/aggregate/decision artifacts, and focused regression tests.
- Remaining: resolve the legacy information box-equation versus first-order proxy, beta unit semantics, causal leakage, and foundation correspondence before downstream physical claims.
- Commit status: this wave is ready for a scoped local commit; unrelated worktree changes remain unstaged.

## Canonical C-I operator and beta semantics closure

- Area: `research-core` (secondary: `research-standards`)
- Scope: the remaining scoped contradictions in the normalized `legacy_variational_v1` lane
- Work: aligned the canonical periodic I operator with the declared C/I functional; kept the historical box/wave wording comparator-only; separated dimensionless `beta_normalized` from SI `E_min = k_B*T*ln(2)`; added derivative/operator/unit regression tests; regenerated compatibility, aggregate, and decision artifacts; synchronized manual reports.
- Verification: legacy closure `PASS_CONDITIONAL` with no scoped blockers; compatibility audit `PASS`/`BLOCKED` only by remaining causal and O(2) dependencies; targeted suite passed `23 tests`; full core suite passed `413 tests` with 8 unrelated pre-existing failures; decision is `CONDITIONAL_SCOPED_CANONICAL` with no scoped hard contradictions.
- Public-safety: `blocked` for global foundation and physical promotion; only the declared normalized lane is conditionally closed.
- Files/artifacts: canonical master operator, parameter contract, audit/build scripts, closure/compatibility/aggregate/decision JSON and reports, focused tests.
- Remaining: matter-space causal leakage, O(2)-to-double-well reduction, complete F0-F3 correspondence/units, observable maps, and legacy standard-limit claims.
- Commit status: intended files remain uncommitted until diff review; unrelated worktree changes remain unstaged.
## Thought experiment source note: observation as past behavior

- Area: `research-core` (secondary: `research-standards`)
- Scope: recorded the user-provided thought experiment linking delayed observation, completed behavior, energy use, persistence, and natural selection.
- Work: created `docs/core/THOUGHT_EXPERIMENT_OBSERVATION_PAST_BEHAVIOR.md` and linked it from the core README; separated conceptual premises from standard-physics correspondence and unresolved UET hypotheses.
- Verification: claim audit applied; no numerical or empirical claim was promoted; the note explicitly records the thought experiment as a conceptual source note.
- Public-safety: `partial`; suitable as a research premise, not as physical validation.
- Remaining: derive the observation operator, propagation-delay model, energy/free-energy ledger, and the mapping from physical response to observable record.
- Commit status: initial thought-experiment note and README link were committed in 1c1d151d2; the Einstein extension is part of the current follow-up wave.

## Einstein correspondence: behavior limit as a special case

- Area: research-core (secondary: research-standards)
- Scope: added the user-provided interpretation that Einstein’s \(c\) limit is one special case of a broader UET constraint on behavior, while separating standard SR equations from the open UET extension.
- Work: extended docs/core/THOUGHT_EXPERIMENT_OBSERVATION_PAST_BEHAVIOR.md with the massive-body energy limit, photon massless branch, free-energy/total-energy distinction, and claim-status table.
- Verification: formula-audit workflow applied; git diff --check passed; no physical status was promoted.
- Public-safety: partial; the Einstein correspondence is standard in its SR portion, while the broader behavior–energy interpretation remains a hypothesis/open derivation target.
- Remaining: define the behavior-rate observable, close the energy/free-energy ledger, and test whether a general constraint can be stated without conflating kinetic energy, rest mass, radiation, and causal speed.
- Commit status: Einstein extension was committed in a2f92e667; the relational Newtonian extension is part of the current follow-up wave.

## Relational Newtonian baseline for observer information

- Area: research-core (secondary: research-standards)
- Scope: formalized the user-provided distinction between a Newtonian two-body/observer relation and the later Einstein causal/Lorentz extension.
- Work: extended docs/core/THOUGHT_EXPERIMENT_OBSERVATION_PAST_BEHAVIOR.md with relative position/velocity, finite-signal delay, observer measurement operator, and the three-layer UET map.
- Evidence read: historical Axiom 1–3 in the 0.2–0.4 equation analysis and the 0.8.5 Source–Transmission–Latency–Observer framework.
- Verification: formula-audit workflow applied; the section separates standard Newtonian/SR correspondence from UET hypotheses and does not promote a physical claim.
- Public-safety: partial; the relational observable model is a conceptual/formalization target, not empirical validation.
- Remaining: implement a two-body finite-signal toy model, test Galilean and Lorentz versions separately, and define which trace is physical versus observer-derived.
- Commit status: this extension remains uncommitted until scoped review.
## Canonical C interaction ontology and physical mapping

- Area: `research-standards` (secondary: `research-core`)
- Scope: documentation-first reconciliation of the historical C/I equation language with the user's relational interaction interpretation.
- Work: reviewed promoted core summaries, the historical master-equation page, practical system-equation guide, notation guide, promotion map, and related legacy/history notes; added `03_Core_Theory/relational-C-and-physical-mapping.md`; corrected current docs so `C` is not universally mass, density, energy, information, or substance.
- Verification: `git diff --check` passed after whitespace repair; claim boundary reviewed; no physical status promoted and no equation/operator code changed.
- Public-safety: `partial`; the ontology is a current research proposal and correspondence contract, not a physical derivation or empirical validation.
- Files/artifacts: canonical relational C note, stable equation summaries, historical equation status notice, practical user guide, notation notice, documentation index, promotion map, and core README link.
- Remaining: derive and test one lane-specific mapping from `C` to a standard physical quantity, beginning with the relational two-body/observer baseline before mass or galaxy claims.
- Commit status: scoped documentation wave is ready for local commit; unrelated worktree changes remain unstaged.
## Relational two-body baseline correspondence wave

- Area: `research-core` (secondary: `research-standards`)
- Scope: first executable foundation correspondence after locking `C` as a relational interaction coordinate.
- Work: added normalized Newtonian two-body comparator, finite-signal observer record, formula audit, focused tests, registry record, specification, update log, and generated artifact.
- Verification: regenerated F0 inventory (`PASS_WITH_DISCLOSED_GAPS`, inventory gate remains `BLOCKED`); relational audit `PASS`; focused suite passed `5 tests`; registry JSON parsed successfully; mapping residuals were `0.0` energy and `2.78e-17` force, energy drift `2.11e-15`, momentum drift `0.0`, Galilean C error `5.77e-15`.
- Public-safety: `partial`; standard comparator is internally checked and simulation-only, not a UET gravity/mass derivation or empirical validation.
- Files/artifacts: `docs/core/RELATIONAL_TWO_BODY_BASELINE_SPEC.md`, `docs/core/relational_two_body_baseline.py`, audit script, test, generated artifact, registry entry, core README link, and local update log.
- Result: geometry-only `C` remains unchanged under mass rescaling while potential/force amplitude scales with mass; mass is therefore kept as a separate standard counterpart parameter in this lane.
- Remaining: choose a dimensional lane (`C -> rho` for mass-density or another constitutive interaction map), define observable/uncertainty policy, and only then consider galaxy/application fitting.
- Commit status: scoped baseline wave remains uncommitted until diff review; unrelated worktree changes remain unstaged.
## Direct C-to-mass-density identifiability wave

- Area: `research-core` (secondary: `research-standards`)
- Scope: test whether the geometry-only relational `C` from the two-body baseline uniquely determines a mass-density observable.
- Work: added normalized Gaussian-kernel density observable, constructive common-mass-rescaling diagnostic, formula audit, focused tests, registry record, specification, update log, and generated artifact.
- Verification: F0 inventory remains `PASS_WITH_DISCLOSED_GAPS` / `BLOCKED`; mass-density audit `PASS_WITH_BLOCKED_MAPPING`; focused suite passed `3 tests`; registry JSON parsed successfully.
- Public-safety: `blocked` for direct C-to-rho physical interpretation; `partial` for the internal identifiability result; no SI units, galaxy data, fitting, uncertainty, or holdout.
- Result: same geometry-only `C=-1` was retained while integrated density changed from `2` to `4` code mass units under a twofold mass rescale; normalized shape residual was `0.0`. A direct `rho=f(C)` map is not identified.
- Remaining: define an independent matter-amplitude/source state and dimensional observable map before considering a mass-density lane or galaxy application.
- Commit status: scoped mass-density correspondence wave remains uncommitted until diff review; unrelated worktree changes remain unstaged.

## Matter-to-interaction forward correspondence wave

- Area: `research-core` (secondary: `research-standards`)
- Scope: forward mapping from an independently declared matter source to density observable, relational `C`, standard pair interaction, force, and acceleration.
- Work: added `docs/core/matter_interaction_forward.py`, specification/update log, focused tests, verifier, generated artifact, registry entry, and core exports/README links.
- Verification: forward artifact `PASS_WITH_UET_EXTENSION_BLOCKED`; all 9 local gates true; focused two-body/density/forward suite passed `11 tests`; equation-foundation audit `PASS` with foundation gate still `BLOCKED`; compatibility audit `PASS` with existing blockers unchanged.
- Result: common source rescaling leaves geometry-only `C=-1` unchanged, density integral scales `2 -> 4`, pair energy/force scale `4x`, and body-A acceleration scales `2x`. This supports a forward source/amplitude separation, not a direct `rho=f(C)` inverse map.
- Public-safety: `blocked` for physical/UET extension promotion; `simulation-only` standard comparator with normalized units and no galaxy/data claim.
- Remaining: declare and audit a non-Newtonian constitutive response plus dimensional observable/uncertainty/holdout map before any galaxy application.
- Commit status: scoped forward wave ready for local commit; unrelated worktree changes remain unstaged and untouched.
